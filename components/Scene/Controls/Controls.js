import Moving from './Moving'
import Rotating from './Rotating'
const Controls = () => {
    
    return (
        <>
        <Moving></Moving>
        <Rotating></Rotating>
        </>
    )
}

export default Controls;
